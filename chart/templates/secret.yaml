apiVersion: v1
kind: Secret
metadata:
  name: {{ include "llmops.fullname" . }}-secret
  labels:
    {{- include "llmops.labels" . | nindent 4 }}
type: Opaque
stringData:
  ANTHROPIC_API_KEY: {{ .Values.secrets.anthropicApiKey | quote }}
  DATABASE_URL: {{ .Values.telemetry.databaseUrl | quote }}
