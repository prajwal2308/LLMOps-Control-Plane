apiVersion: v1
kind: Service
metadata:
  name: llmops-control-plane
  namespace: llmops
  labels:
    app.kubernetes.io/name: llmops-control-plane
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  selector:
    app: llmops-control-plane
